job_market_dbt:
  target: dev
  outputs:
    dev:
      type: bigquery
      method: service-account
      project: your-gcp-project-id
      dataset: job_market_dev
      keyfile: /path/to/service-account.json
      threads: 4
      timeout_seconds: 300
      location: US
      priority: interactive